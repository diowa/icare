it-IT:
  mongoid:
    errors:
      messages:
        invalid_date: "non è una data valida"
        invalid_time: "non è un orario valido"
        invalid_datetime: "non è una data valida"
        is_at: "deve essere uguale a %{restriction}"
        before: "deve essere antecedente a %{restriction}"
        on_or_before: "deve essere uguale o precedente a %{restriction}"
        after: "deve essere successiva a %{restriction}"
        on_or_after: "deve essere uguale o successiva a %{restriction}"
  validates_timeliness:
    error_value_formats:
      date: '%d-%m-%Y'
      time: '%H:%M:%S'
      datetime: '%d-%m-%Y %H:%M:%S %Z'
